# Matchsticks — Worker (Phase 2)

This directory will contain the Cloudflare Worker for the leaderboard backend.

**Phase 2 scope:**
- `POST /score` — submit a score (HMAC-signed, rate-limited)
- `GET /top?limit=10` — top scores
- `GET /me/:username` — personal bests

Not yet implemented. Leave this folder alone until Phase 2.
