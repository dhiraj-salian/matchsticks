# Matchsticks — Deploy (Phase 3)

This directory will contain GitHub Pages deployment configuration.

**Phase 3 scope:**
- GitHub Actions workflow: build → deploy to `gh-pages` branch
- DodoPayments integration for ad-free badge

Not yet implemented. Leave this folder alone until Phase 3.
